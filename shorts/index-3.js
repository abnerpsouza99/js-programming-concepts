const person = {
    name: 'Abner',
    ytChannel: 'Canal - Eu Sou Programador',
    age: 23,
    subscribers: 102
};

// Capturando chaves de um objeto
const keys = Object.keys(person);

console.log(keys);

// Capturando valores de um objeto
const values = Object.values(person);

console.log(values);

